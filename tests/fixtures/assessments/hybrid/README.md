# Hybrid MVP dashboard visibility fixture

Use `dashboard-visibility-test-hybrid.json` in the admin package import flow to validate the full `hybrid_mvp_v1` path (import → publish → assign → complete → dashboard/workspace/result visibility).

This fixture is intentionally minimal but complete for deterministic, manual end-to-end smoke testing.
